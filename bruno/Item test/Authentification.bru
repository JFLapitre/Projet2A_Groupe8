meta {
  name: Authentification
  type: http
  seq: 1
}

post {
  url: https://user-joanes-509816-user-5000.user.lab.sspcloud.fr/auth?username=groupe8&password=adminpass321
  body: none
  auth: none
}

params:query {
  username: groupe8
  password: adminpass321
}

script:post-response {
  const response = res.getBody();
  bru.setEnvVar("AUTH_TOKEN", response.access_token);
  console.log("Token stocké:", response.access_token);
}

settings {
  encodeUrl: true
  timeout: 0
}
