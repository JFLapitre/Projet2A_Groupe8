meta {
  name: GET Item 1
  type: http
  seq: 3
}

get {
  url: {{BASE_URL}}/items/1
  body: none
  auth: inherit
}

assert {
  res.body: isJson
  res.body.name: eq "Banh mi"
}

settings {
  encodeUrl: true
  timeout: 0
}
